# ─── Supabase ──────────────────────────────────────────────────────────────────
# URL del proyecto (Dashboard → Settings → API → Project URL)
SUPABASE_URL=https://xxxxx.supabase.co
# Anon key — público, seguro para frontend (Dashboard → Settings → API → anon public)
SUPABASE_KEY=eyJ...
# Service role key — NUNCA exponer en frontend (Dashboard → Settings → API → service_role)
SUPABASE_SERVICE_ROLE_KEY=eyJ...
# Project ref — ID del proyecto (visible en la URL del dashboard)
SUPABASE_PROJECT_REF=gmnrfuzekbwyzkgsaftv
# Management API key — para CLI y migraciones (Dashboard → Account → Access Tokens)
SUPABASE_MANAGEMENT_API_KEY=sbp_...

# ─── Cloudinary ────────────────────────────────────────────────────────────────
# Cloud name (Dashboard → Settings)
CLOUDINARY_CLOUD_NAME=
# Upload preset unsigned (Dashboard → Settings → Upload → Upload presets)
CLOUDINARY_UPLOAD_PRESET=
# API key (Dashboard → Settings → Security)
CLOUDINARY_API_KEY=
# API secret (Dashboard → Settings → Security) — NUNCA exponer en frontend
CLOUDINARY_API_SECRET=

# ─── Stripe ────────────────────────────────────────────────────────────────────
# Clave secreta (Dashboard → Developers → API keys → Secret key)
STRIPE_SECRET_KEY=sk_test_...
# Webhook secret (Dashboard → Developers → Webhooks → Signing secret)
STRIPE_WEBHOOK_SECRET=whsec_...

# ─── Resend (Email) ───────────────────────────────────────────────────────────
# API key (resend.com → API Keys)
RESEND_API_KEY=re_...

# ─── Cloudflare Turnstile ──────────────────────────────────────────────────────
# Secret key del widget (Cloudflare Dashboard → Turnstile → Settings)
TURNSTILE_SECRET_KEY=0x...
# Site key del widget — público (Cloudflare Dashboard → Turnstile → Settings)
TURNSTILE_SITE_KEY=0x...

# ─── Anthropic AI ──────────────────────────────────────────────────────────────
# API key para generación de contenido (console.anthropic.com → API Keys)
ANTHROPIC_API_KEY=sk-ant-...

# ─── AI Model Configuration (override defaults from aiConfig.ts) ─────────────
AI_MODEL_FAST=claude-3-5-haiku-20241022
AI_MODEL_VISION=claude-sonnet-4-5-20250929
AI_MODEL_CONTENT=claude-sonnet-4-5-20250929

# ─── AI Fallback: OpenAI (optional — enables failover) ──────────────────────
OPENAI_API_KEY=
AI_FALLBACK_MODEL_FAST=gpt-4o-mini
AI_FALLBACK_MODEL_VISION=gpt-4o
AI_FALLBACK_MODEL_CONTENT=gpt-4o

# ─── Site Configuration ──────────────────────────────────────────────────────
SITE_URL=https://tracciona.com
SITE_NAME=Tracciona

# ─── WhatsApp Business API ────────────────────────────────────────────────────
# Token de acceso permanente (Meta Business Suite → System Users → Generate Token)
WHATSAPP_API_TOKEN=
# Phone Number ID (Meta → WhatsApp → Phone numbers)
WHATSAPP_PHONE_NUMBER_ID=
# Verify token — string arbitrario para validar webhook (tú lo defines)
WHATSAPP_VERIFY_TOKEN=
# App Secret — para verificar firma de webhooks (Meta → App Dashboard → Settings → Basic)
WHATSAPP_APP_SECRET=

# ─── Cron Job Secret ──────────────────────────────────────────────────────────
# Token arbitrario para proteger endpoints /api/cron/* (genera uno con openssl rand -hex 32)
CRON_SECRET=

# ─── Quaderno (Facturación) ───────────────────────────────────────────────────
# API key (quaderno.io → Account → API)
QUADERNO_API_KEY=
# API URL — normalmente https://quadernoapp.com/api
QUADERNO_API_URL=https://quadernoapp.com/api

# ─── Push Notifications (VAPID) ───────────────────────────────────────────────
# Private key VAPID (generar con npx web-push generate-vapid-keys)
VAPID_PRIVATE_KEY=
# Email de contacto para VAPID
VAPID_EMAIL=mailto:admin@tracciona.com
# Public key VAPID — se expone al frontend (generar junto con la private key)
NUXT_PUBLIC_VAPID_PUBLIC_KEY=

# ─── Cloudflare ───────────────────────────────────────────────────────────────
# API Token con permisos de zona (Dashboard → Profile → API Tokens)
CLOUDFLARE_API_TOKEN=
# Account ID (Dashboard → Overview → lado derecho)
CLOUDFLARE_ACCOUNT_ID=
# Cloudflare Images — API Token específico para Images
CLOUDFLARE_IMAGES_API_TOKEN=
# Cloudflare Images — Account hash (Dashboard → Images → Overview)
CLOUDFLARE_IMAGES_ACCOUNT_HASH=
# Cloudflare Images — Delivery URL base
CLOUDFLARE_IMAGES_DELIVERY_URL=

# ─── Sentry (Error Tracking) ──────────────────────────────────────────────────
# Organización slug (sentry.io → Settings → Organization)
SENTRY_ORG_SLUG=
# Auth token para releases (sentry.io → Settings → Auth Tokens)
SENTRY_AUTH_TOKEN=
# DSN público — se expone al frontend (sentry.io → Project → Settings → Client Keys)
NUXT_PUBLIC_SENTRY_DSN=

# ─── Google ────────────────────────────────────────────────────────────────────
# OAuth Client ID (Google Cloud Console → Credentials)
GOOGLE_CLIENT_ID=
# Google Ads ID — se expone al frontend (Google Ads → Account → Settings)
NUXT_PUBLIC_GOOGLE_ADS_ID=

# ─── Image Pipeline ───────────────────────────────────────────────────────────
# Modo de procesamiento de imágenes: cloudinary | hybrid | cf_images_only
IMAGE_PIPELINE_MODE=cloudinary

# ─── Infrastructure Alerts ────────────────────────────────────────────────────
# Email para recibir alertas de infraestructura
INFRA_ALERT_EMAIL=admin@tracciona.com
# Umbrales de alerta (porcentaje de uso, 0-100)
INFRA_ALERT_THRESHOLD_WARNING=70
INFRA_ALERT_THRESHOLD_CRITICAL=85
INFRA_ALERT_THRESHOLD_EMERGENCY=95

# ─── Vertical ─────────────────────────────────────────────────────────────────
# Identificador del vertical activo (e.g. 'tracciona', 'tankiberica')
NUXT_PUBLIC_VERTICAL=tracciona
